/*
LEGACY REGISTRATION PAGE (archived)

This file used to contain the full previous `Registration.tsx` implementation.
The repo’s TypeScript build treats unused locals/imports as errors, so the legacy
implementation is kept as an archive note instead of live code.
*/

import { Typography } from "@/components/common/Typography";

export default function RegistrationPageOldPlaceholder() {
  return (
    <div className="rounded-md border border-[#E8E8E8] bg-white p-6 text-center">
      <Typography variant="body">
        Old registration implementation is archived for reference.
      </Typography>
    </div>
  );
}

