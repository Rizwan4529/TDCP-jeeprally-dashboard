import { Typography } from "@/components/common/Typography"

export default function EventsPage() {
  return (
    <div className="space-y-2">
      <Typography as="h2" variant="h5">
        Events
      </Typography>
      <Typography color="muted">Events page placeholder.</Typography>
    </div>
  )
}
